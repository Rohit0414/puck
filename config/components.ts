// config/components.ts
import FormWidget from '../components/FormWidget';

const components = [
  // Other components can be listed here
  {
    id: 'form',
    name: 'Form',
    component: FormWidget,
    icon: 'form-icon', // Optional icon reference
  },
];

export default components;
